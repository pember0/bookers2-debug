<div class='container'>   <!-- containerは、レスポンシブな固定幅のコンテナ -->
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0">
      <h2>Sign up</h2>

      <!-- フォームを作成するヘルパーとしてform_forは現在非推奨のためform_withへと変更-->
      <!-- ユーザー登録画面 -->
      <%= form_with model: @user, url: user_registration_path do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <!-- ユーザ登録の際に name 登録するため、フォーム  -->
        <div class="field">
          <%= f.label :name %><br>
          <%= f.text_field :name %>
        </div>

        <!-- ユーザ登録の際に email 登録するため、フォーム  -->
        <div class="field">
          <%= f.label :email %><br>
          <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :password %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "new-password" %>
        </div>
        <div class="field">
          <%= f.label :password_confirmation %><br />
          <%= f.password_field :password_confirmation, autocomplete: "off", class: "password_confirm" %>
        </div>

        <!-- 「Sign up」ボタンの色を緑色、サイズは、btn-sm(小さめ) -->
        <div class="actions">
          <input type="submit" name="commit" value="Sign up" class="btn btn-success btn-sm" />
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>